# Venture Lab — coordinator heartbeat
updated: 2026-07-16T17:13:17Z

**Slice this wake: shrank the owner's cold NL proofread read to a guided checklist, without touching the owner gate (PR #214, ⚑ self-initiated, the session's FINAL planned round).** The binding constraint on the catalog's ~13 ready NL editions is a single owner-only native-speaker proofread pass — an AI cannot clear it (the #213 slice already recognised it as HARD-GATED). This slice does NOT touch that gate; it attacks the constraint sideways with artifacts that make the owner's read guided instead of cold. Part A: a mechanical `PRE-QA.md` pre-annotation note (sibling to each version's `NOTES.md`) for the four NL titles closest to publishable — De geborgen boomgaard (hen/hen/hun pronoun-strategy consistency with 43 antecedent-check spots, the Enthoutbibliotheek capitalization split, verbatim-motif punctuation drift), De Marmeladepost (single-mint coinage confirmed, 26-site inner-quote inventory, je/u register), De zoete zee (craft-neologism inventory, the steunwet capitalization checked-intentional, the tightest +1.5% expansion seams), and Liefde in de kantlijn (epistolary register-arc map with the non-monotonic closing spots, the belasting load/tax dual-sense sites). Every citation is a real manuscript location; hunspell nl_NL is not installed, so NO spellcheck pass was claimed — consistency and mechanical checks only, and each note leads with a disclaimer that it is machine pre-annotation, not a native proofread, and does NOT clear the §7 gate. Part B: a length-band ruling PREP doc for De Morgendeur (16,730 NL / 15,995 EN) and De Oogstslag (24,655 NL / 23,334 EN) against the ~20k–30k packet band, with a one-word-ratify recommendation (keep current lengths; De Oogstslag already sits in band) — prep only, the ⚑ length-band ruling stays the owner's. Part C: a same-class owner-wording fix — a native-speaker proofread blocking row is never classified as executing a D-item decision above, so Weduwenblauw's HARD-GATED suffix now names its proofread row instead of the phantom "(a D-item above blocks this sequence)"; the genuinely D-item-linked illustration books keep their correct wording. The seat performed no publish click and ticked no owner gate.

**Boot facts (synced at HEAD):** main HEAD at boot `9473e5f` (PR #213 — recognised the ⚑ Owner native-speaker proofread gate as HARD-GATED); `git fetch origin && git reset --hard origin/main` clean at boot; inbox re-read at HEAD — still ends at ORDER 015 (all acked at the 2026-07-15 reboot), no unexecuted `new` ORDER, and `control/inbox.md` untouched this wake. PR #214 opened this wake (READY, base main, head `claude/pre-qa-notes`) against a born-red session card that holds substrate-gate red by design until the completion flip; the seat does NOT arm auto-merge or self-merge — the enabler lands it. The OWNER-QUEUE regenerated via the script; hard-gated count stays 19 (Part C changed only Weduwenblauw's explanatory wording — no gate flipped). No live claim beyond this slice's own (`control/claims/2026-07-16-pre-qa-notes.md`).

**Seat cadence:** this seat runs wake-less — a coordinator-dispatched slice off the owner-queue surface (the #213 follow-on into the binding proofread constraint), no pacemaker tick drove it. The routine/baton facts below are carried forward from the prior heartbeat, not re-verified this wake; treat them as pointers, not fresh readings.

**DRY-BACKLOG (honest):** net-new inventory is paused. The single binding lever on the ~13 ready NL editions is the owner-only native-speaker proofread pass, which an AI cannot clear — and this wake spent its value squeezing that read down rather than adding to the pile. The remaining agent-executable work around the NL catalog is now diminishing-value (the pre-QA notes, once written for the closest titles, do not multiply cheaply — the rest is padding an owner already told us needs a human). This was the session's FINAL planned round; the honest next move is to hold for the owner's proofread/length-band/title-ratify decisions rather than manufacture more agent churn.

**Baton item 1 — Friday 2026-07-17 grading executor (carried, held):** trading-strategy annex lane owns it as its own `next_1`; grading cron `trig_01BsYsMABu2vfH4d2MzuSLs6` (`0 9 * * 5`, next 2026-07-17T09:08Z) is session-bound to the archived session; `grade_paper.py` is idempotent and a no-op until ~early August; foreign `trig_01YXNmgqYeYQ1LuepsLmbNCG` already covers the 09:00Z window. Coordinator re-check due ~T-12h (≈2026-07-16T21:00Z): escalate to an archive-proof rebind only if the annex lane is then found stalled with the executor still unsecured.

**Kill clocks (carried):** Stripe Webhook Test Kit ⏲ 2026-07-19 T+7 funnel checkpoint · ⏲ 2026-07-26 T+14 kill-rule — 0 overdue, 0 due today.

⚑ owner (carried forward, still live):
- Project custom instructions are dictionary v3.4 vs registry v3.6 — re-paste from fm:projects/venture-lab/instructions.md.
- Publish clicks queued and untouched: `docs/publishing/OWNER-QUEUE.md` — 45 click-run sequences, 19 hard-gated, totalling 268 unchecked owner clicks; no click performed this wake and no gate ticked.
- The binding lever: the owner-only native-speaker proofread pass on the ready NL editions. Four of the closest titles now carry a mechanical `PRE-QA.md` checklist to guide that read (De geborgen boomgaard, De Marmeladepost, De zoete zee, Liefde in de kantlijn); the read itself remains the owner's.
- Length-band ruling awaiting a one-word ratify: `candidates/adult-novels/the-night-kiln/LENGTH-BAND-PREP.md` (De Morgendeur / De Oogstslag).

**Next-2 (baton):**
1. Friday 2026-07-17T09:00Z grading: at the next wake inside ~T-12h, re-check the annex lane; if the executor is still unsecured and no live wake will reach it, coordinator rebinds an archive-proof (fresh-session-per-fire) grading trigger (idempotency makes a duplicate fire harmless).
2. Empty inbox + no unexecuted ORDER, and net-new inventory at diminishing value → hold for owner decisions (proofread pass, length-band ratify, title ratifies) rather than manufacture another write-slice; take only repo-health/hygiene slices as they surface from the strict gate.

kit: v1.17.0
