# Venture Lab — coordinator heartbeat
updated: 2026-07-16T16:47:09Z

**Slice this wake: fixed an understatement bug on the owner-facing publish queue — three NL editions were shown click-ready to publish when they still need the same blocking native-speaker proofread pass (PR #213, ⚑ self-initiated).** `scripts/derive_owner_queue.py` decided a click-run was HARD-GATED purely by whether the literal word "blocking" appeared in an owner checkbox (`blocked = any("blocking" in b.lower() for b in owner_boxes)`). De Waag, Het trage woord, and Weduwenblauw carry the identical owner native-speaker proofread quality gate as every other NL edition, but their §7 proofread checkbox read `native-speaker proofread pass approved/commissioned.` with no `(blocking quality gate for this title)` continuation — so all three rendered as NOT hard-gated, telling the owner they were click-ready to publish while they still required the blocking human proofread pass (a real quality/reputation risk on an un-proofread NL edition). Root-cause fix in the generator: hard-gate detection now recognises a native-speaker proofread pass as inherently blocking regardless of the literal keyword (`is_blocking_box` helper), preserving existing behaviour for checkboxes that already say "blocking" and scoped so unrelated ⚑ Owner rows (title ratify, cover, price, the publish click) are not mis-flagged. The three packets were also normalised to the house form used by de-nachtoven (+~10 other NL packets). Test extended (a proofread-gate box WITHOUT the keyword is detected hard-gated + a regression that ordinary ⚑ Owner rows are not mis-flagged); `OWNER-QUEUE.md` regenerated via the script (16 → 19 hard-gated sequences); `current-state.md` count synced. The seat performed no publish click.

**Boot facts (synced at HEAD):** main HEAD at boot `d1ecd18` (PR #212 — pruned stale claims for merged #210/#211); `git fetch origin && git reset --hard origin/main` clean at boot; inbox re-read at HEAD — ends at ORDER 015 (all acked at the 2026-07-15 reboot), no unexecuted `new` ORDER. PR #213 opened this wake (READY, base main, head `claude/proofread-gate-detection`) against a born-red session card that holds substrate-gate red by design until the completion flip; the seat does NOT arm auto-merge or self-merge — the enabler lands it. No live claim beyond this slice's own (`control/claims/2026-07-16-proofread-gate-detection.md`).

**Seat cadence:** this seat runs wake-less — a coordinator-dispatched hygiene/bugfix slice off the owner-queue surface (the #210/#211 follow-on investigation), no pacemaker tick drove it. The routine/baton facts below are carried forward from the prior heartbeat, not re-verified this wake; treat them as pointers, not fresh readings.

**Baton item 1 — Friday 2026-07-17 grading executor (carried, held):** trading-strategy annex lane owns it as its own `next_1`; grading cron `trig_01BsYsMABu2vfH4d2MzuSLs6` (`0 9 * * 5`, next 2026-07-17T09:08Z) is session-bound to the archived session; `grade_paper.py` is idempotent and a no-op until ~early August; foreign `trig_01YXNmgqYeYQ1LuepsLmbNCG` already covers the 09:00Z window. Coordinator re-check due ~T-12h (≈2026-07-16T21:00Z): escalate to an archive-proof rebind only if the annex lane is then found stalled with the executor still unsecured.

**Kill clocks (carried):** Stripe Webhook Test Kit ⏲ 2026-07-19 T+7 funnel checkpoint · ⏲ 2026-07-26 T+14 kill-rule — 0 overdue, 0 due today.

⚑ owner (carried forward, still live):
- Project custom instructions are dictionary v3.4 vs registry v3.6 — re-paste from fm:projects/venture-lab/instructions.md.
- Publish clicks queued and untouched: `docs/publishing/OWNER-QUEUE.md` — 19 decisions + 45 click-run sequences totalling 268 unchecked owner clicks, now **19 hard-gated** after this wake's proofread-gate fix (De Waag, Het trage woord, and Weduwenblauw joined the hard-gated set — up from 16 — since their blocking native-speaker proofread pass is now recognised). No click performed this wake.

**Next-2 (baton):**
1. Friday 2026-07-17T09:00Z grading: at the next wake inside ~T-12h, re-check the annex lane; if the executor is still unsecured and no live wake will reach it, coordinator rebinds an archive-proof (fresh-session-per-fire) grading trigger (idempotency makes a duplicate fire harmless).
2. Empty inbox + no unexecuted ORDER → standing default: the next generative BOOKS write-slice picks the next-ranked unwritten shortlist concept — **#4 The Lamp Room (24/30)** — or repo-health/hygiene slices as they surface from the strict gate.

kit: v1.17.0
