# Title Vetting — <Book Title>

> **Status:** plan
>
> One packet per title, worked top to bottom BEFORE any listing step.
> A title that hasn't passed this packet is *written*, not
> *publishable*. Every step that touches an account, money, or a
> publish button belongs in §7 as a human decision — pre-pick sane
> defaults so "go with defaults" works.

**Title:** <working title> · **Category:** <genre / shelf> ·
**Date vetted:** <YYYY-MM-DD>

## 1. Title

- [ ] Working title: **<title>**. Subtitle: <subtitle, or "none">
      — decided, not defaulted (a subtitle is often the §2
      mitigation; if so, mark it MANDATORY).
- [ ] Searchability notes: <bare-word drift, misspellings, unwanted
      adjacencies>.

## 2. Collision scan (discoverability, not legality)

Search the exact title as a buyer would; record what surfaced:

| Title | Collision | Finding (what surfaced, with links + access date) | Action |
|---|---|---|---|
| <title> | None / Low / Moderate / Strong | <...> | <keep / keep + mandatory subtitle / rename> |

- [ ] Verdict: **<severity>** — and WHICH failure mode (a competing
      book vs a crowded common-noun search space are different
      problems; record which).
- [ ] Mitigation locked now (if any): <...>

## 3. Market verification

- [ ] Length class: <planned band> — planned vs **measured** (state
      which; if the manuscript doesn't exist yet, say so — this
      packet then PARKS at "no manuscript" honestly).
- [ ] Comps: <2–3 comparable titles at your target length/price, with
      links + access dates>.
- [ ] Series/KU context: <standalone / series; exclusivity call and
      why; reversibility noted>.
- [ ] Seasonality stated plainly: <window, or "none">.
- [ ] Revenue framing: **unknown-author base case ≈ $0.** Unit
      economics only: <price> at <royalty %> ≈ <$/sale>. No
      projections.

## 4. Price band

- [ ] Band: <verified band for the category> → **recommended
      <$X.XX>** with the royalty-tier arithmetic: <...>.

## 5. Packaging (production gate)

- [ ] Gate: <cover-only / illustrated — illustration is a MONEY
      decision, flag it>.
- [ ] Format spec: <ebook spec, cover dimensions, direction in one
      line>.
- [ ] True length: <measured `wc -w`, or "unconfirmed — manuscript
      does not exist" (flagged, not skipped)>.

## 6. Listing copy (drafted in-repo — nothing published)

- [ ] Blurb — rewritten from the FINISHED text, keeping every promise
      the concept blurb made: <...>
- [ ] Two categories: <...> (checked against your catalog's keyword
      allocation — no cannibalizing an owned node).
- [ ] Seven keywords, register-checked: <...>

## 7. Publish clicks (queued for the human — never automated)

**ACTION — Publish "<title>" at <$X.XX>**
1. **Account:** <storefront/KDP account + tax/bank state>.
2. **Cover:** <approval + any spend — a money decision>.
3. **Format/upload:** <file, subtitle applied, title recheck at
   upload>.
4. **Price:** **<$X.XX>** (default per §4).
5. **Categories/keywords:** per §6, re-verified at upload.
6. **Publish + record:** publish, copy the public URL, then record
   the launch durably (URL, price, timestamp) and ARM the kill clock
   (guide chapter 7): T+7 <date> first-signal check · T+30 <date>
   validation-signal deadline.

- [ ] **Owner/you:** account ready.
- [ ] **Owner/you:** cover approved / spend authorized.
- [ ] **Owner/you:** price set (**<$X.XX>** (default)).
- [ ] **Owner/you:** launch-timing call <esp. if §3 is seasonal>.
- [ ] **Owner/you:** the publish click.
- [ ] Post-click: launch recorded + kill clock armed.

---

**Verdict:** <publish-ready up to the click gate / PARKS at <blocker>>
— plus the honest caveats: <what is NOT proven>.
