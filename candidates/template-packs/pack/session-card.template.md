# Session — <FILL: short session title>

> **Status:** `in-progress`
>
> Born red. Create this card as your FIRST commit with `in-progress`, then flip
> to `complete` as the deliberate LAST step once the close-out is written. A
> half-finished session must never read as done.

- **📊 Model:** <FILL: model id, e.g. claude-opus-4-8> · <FILL: effort/mode> · <FILL: session slug>
- **session:** <FILL: one line — what this session is trying to achieve>
- **started (date -u):** <FILL: paste output of `date -u`>
- **completed (date -u):** <FILL: paste output of `date -u` at close — leave blank while in-progress>

## ⟲ Previous-session review

⟲ previous-session review: <FILL: what the last session shipped and how this one
builds on it — reference the last PR/commit. One or two sentences. This is the
memory handoff; skipping it makes every session start cold.>

## 💡 Session idea

💡 <FILL: the one non-obvious insight from this session worth carrying forward —
a moat, a footgun, a pattern. Not a task summary; the thing you'd want the next
session to know that isn't obvious from the diff.>

## Log

- <FILL: chronological notes — synced to X, branched Y, built Z, proved it with
  command C → result. Terse is fine; this is the evidence trail.>

## Deliverable summary

<FILL: 2–3 sentences — what shipped, verified how, and what (if anything) is
deferred or gated as a follow-up. Be honest about scope.>
