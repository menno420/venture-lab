# Ship-It Bundle ($59) — owner bundle-creation click-script

> **Status:** `reference`
>
> **HARD-GATED (2026-07-13):** a Gumroad bundle is assembled FROM existing
> live products — it cannot be created while its components are unpublished.
> The ⚑B (membership-kit $49, click-script
> [`../membership-kit/owner-actions.md`](../membership-kit/owner-actions.md),
> queued PR #106) and ⚑D (template-packs $19 PWYW, click-script
> [`../template-packs/owner-actions.md`](../template-packs/owner-actions.md),
> queued PR #108) publish clicks must be executed FIRST. Nothing below the
> blocking rows in the [§7 packet](../../publishing/vetting/bundle-starter.md)
> proceeds until both component products are live. This is a click-script for
> the owner, not a request to any agent; no agent performs
> publish/spend/account actions.

### ⚑ — Create the "Ship-It Bundle" at $59 · HARD-GATED on ⚑B + ⚑D

- **WHAT:** Create a Gumroad **bundle** priced **$59** (one-time) combining the
  two already-live component products — Membership-Site Boilerplate Kit ($49)
  and Agent-Workflow Template Pack ($19 PWYW) — using the copy in
  [`listing-copy.md`](listing-copy.md). No new file is uploaded: a Gumroad
  bundle references the component products' existing uploads, so the artifacts
  the buyer receives are exactly the component zips already pinned —
  `membership-kit-v0.2.zip` sha256
  `9f262fc84008ad7b1517116ef999c331672d756f6d68fe5378682e38e1d5d3e1` and
  `template-packs-v0.1.zip` sha256
  `d65d4c9ef4b23f3ef7fed7277ef6d73f659891d83773eac9d27e86e35463a2b3`
  (both re-verified against the committed dists 2026-07-13).
- **WHERE:** gumroad.com → *Products* → *New product* → *Bundle* (Gumroad's
  bundle product type lists your existing products as selectable components),
  signed into the same account that published ⚑B and ⚑D.
- **HOW:** 1) Execute ⚑B and ⚑D first (their own click-scripts; blocking).
  2) Sign in to the same Gumroad account. 3) New product → **Bundle**.
  4) Name = "Ship-It Bundle — Membership-Site Kit + Agent-Workflow Template
  Pack". 5) Select the two live products as the bundle contents. 6) Price =
  **$59**, one-time (fixed — do NOT inherit the pack's PWYW setting for the
  bundle). 7) Paste Title / Short + Long description / Bullets / FAQ from
  [`listing-copy.md`](listing-copy.md). 8) Publish. 9) Copy the public
  bundle URL.
- **WHY:** A third price point over the same two artifacts at zero new build
  cost: $59 removes a decision for buyers who want both, an honest $9 under
  the $68 separate total ([`candidates/BUNDLE-LISTING.md`](../../../candidates/BUNDLE-LISTING.md)
  § Pricing). Conservative expectation (Q-0259.4): 0–2 sales/month absent
  distribution; the bundle adds a price point, not a new audience.
- **UNBLOCKS:** The bundle revenue path; the "refresh the $59 bundle listing"
  post-click seat row already queued in the template-packs packet
  ([`../../publishing/vetting/template-packs.md`](../../publishing/vetting/template-packs.md) §7)
  then closes against a live bundle instead of a stale candidate doc.
- **VERIFIED-WHEN:** The public URL loads a purchasable $59 bundle page
  listing BOTH components, and the storefront's own preview/test purchase
  delivers both zips (spot-check the downloads against the two sha256 pins
  above). HARD-GATE stands until then: no bundle click is actionable while
  either ⚑B or ⚑D is unexecuted.
