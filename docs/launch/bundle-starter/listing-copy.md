# Ship-It Bundle ($59) — listing copy

> **Status:** `reference`
>
> Ready-to-paste copy for a **Gumroad bundle** of the two component products,
> refreshed 2026-07-13 from [`candidates/BUNDLE-LISTING.md`](../../../candidates/BUNDLE-LISTING.md)
> to catalog parity (Title / Short ≤200 / Long / Bullets / FAQ — the
> membership-kit / template-packs / SWTK listing shape). **HARD-GATED:** this
> bundle references the two component products as existing live Gumroad
> products — it cannot be created until the ⚑B (membership-kit $49) and ⚑D
> (template-packs $19 PWYW) publish clicks have been executed
> (queued by PR #106 / PR #108). No new zip ships; the component artifacts are
> the pins (shas in the [§7 packet](../../publishing/vetting/bundle-starter.md) §1).

## Title
```
Ship-It Bundle — Membership-Site Kit + Agent-Workflow Template Pack
```

## Short description (≤ 200 chars)
```
The $49 Membership-Site Boilerplate Kit and the $19 Agent-Workflow Template Pack together for $59 — the tested product layer plus the agent discipline that built it. $9 less than separately.
```

## Long description
```
Two small products that do different jobs and pair naturally.

The Membership-Site Boilerplate Kit is the product layer: the whole purchase-to-access loop — Stripe checkout, webhook, membership grant, gated content, invite-on-purchase — already wired and covered by a shipped test suite (36 tests run green from the extracted bundle), demoable in one command with zero accounts.

The Agent-Workflow Template Pack is the workflow layer: the agent constitution, advisory fail-open hooks, session-log card, and one-page playbook that keep a coding agent oriented, honest, and recoverable — extracted from a real autonomous fleet's retros.

Buy both to run a disciplined agent that ships a sellable product. These two actually came from the same place: the template pack is the literal discipline an agent fleet used while building the membership kit. You get the artifact and the method that produced it.

Same honest v0.x scope as the individual listings. The kit's membership flow and persistence are real and tested; live Stripe, Discord, and hosted Supabase are wired but gated behind your own keys. The pack is conventions and advisory reminders, not enforcement. Nothing here is hype.
```

## Bullets
```
- Membership-Site Boilerplate Kit ($49 alone): stdlib-only Python backend (mock + real-Stripe modes), styled landing page, gated members stub, file-backed persistence, shipped 36-test suite — runs with zero accounts
- Agent-Workflow Template Pack ($19 suggested alone): starter agent constitution, three advisory fail-open Claude Code hooks, session-log card, one-page playbook — no deps, no runtime
- $59 for both vs $68 bought separately — an honest $9 nudge, not a manufactured discount anchor
- Both ship as clean, one-command-runnable zips with a top-level QUICKSTART
- Neither depends on the other: the kit runs standalone, the pack is stack-agnostic
- The pack is the literal workflow that built the kit — product + process in one purchase
```

## FAQ
```
Q: Can I buy just one?
A: Yes — each is sold separately (kit $49, pack $19 pay-what-you-want). The bundle is only for people who want both; it saves $9 and one checkout.

Q: Do they depend on each other?
A: No. The kit runs standalone; the pack is stack-agnostic and works with any repo. They pair — one is a product to sell, the other is how your agent should work — but neither requires the other.

Q: Is this production-ready?
A: It's an honest v0.x, same as the individual listings. The kit's membership flow and persistence are real and tested; live Stripe, Discord, and hosted Supabase are wired but gated behind your own keys. The pack is conventions and advisory reminders, not enforcement. You're buying a tested head start and a proven workflow shape, not a black box.

Q: What exactly do I download?
A: The same two zips the individual listings sell: membership-kit-v0.2.zip and template-packs-v0.1.zip. The bundle adds nothing and removes nothing — it's the two products, one checkout, $9 off.

Q: License / support?
A: Same as the component listings — single-project starter license, best-effort community-level support (owner-to-set placeholders on the component pages apply here identically).
```

## Pricing (cited)

**$59** for the bundle vs **$68** bought separately — $49 membership-kit
([`candidates/membership-kit/LISTING.md`](../../../candidates/membership-kit/LISTING.md))
plus $19 suggested for the PWYW template pack
([`candidates/template-packs/LISTING.md`](../../../candidates/template-packs/LISTING.md)),
per [`candidates/BUNDLE-LISTING.md`](../../../candidates/BUNDLE-LISTING.md) § Pricing.
The $9 discount is a modest, honest nudge — the pack is pay-what-you-want on its
own, so the bundle simply removes a decision for buyers who want the whole
build-with-discipline setup. Conservative expectation (Q-0259.4 framing): the
bundle inherits the components' 0–2 sales/month-absent-distribution outlook; it
adds a price point, not a new audience.
