# 2026-07-12 — kit upgrade v1.14.0 → v1.15.0

> **Status:** `complete`

Upgraded substrate-kit v1.14.0 → v1.15.0 (distribution-only wave B; no lane domain work).

📊 Model: fable-5

## What shipped

- `bootstrap.py` swapped to v1.15.0 release asset (sha256 `25d22af9…650e`, 828,825 bytes, verified against the three-way-verified release facts; clean first-try download).
- Old dist banked at `.substrate/backup/bootstrap-1.14.0.py` (sha256 `47c1b8b9…e581`); all 11 pre-existing banks byte-identical.
- Two-command flow: `upgrade` then `upgrade --apply-docs` (mandatory). `--apply-docs` applied CONSTITUTION.md, docs/AGENT_ORIENTATION.md, docs/SKILLS.md.
- Planted: `docs/ROUTINES.md` (trigger/wake doctrine) — wired, no `[reachable]` orphan; `docs/seat-digest.md` (derived render, freshly generated).
- Carve-out scans ran, 0 found; live gate + enabler kept (kit-owned, already current — no gate regen this wave).
- `check --strict`: only red = designed born-red hold on this card.

## Lane-owed follow-ups (not forced by this distribution PR)

- capability-seed HAND-ADOPT-ONCE on docs/CAPABILITIES.md (consumer-diverged ledger, no kit fence — carried over from v1.14.0; verbatim line in PR #83 body).
- Diverged manual merges: control/README.md (chronic), control/status.md (NEW this wave) — deltas preserved in `.substrate/upgrade-report.md` § Template deltas.
- Q-0270 local-prose-copy collapse check; heartbeat `kit:` bump in control/status.md (chronic).

💡 Session idea: the "Lane-owed follow-ups" section of upgrade reports is overwritten wholesale each wave, so chronic items (capability-seed hand-adopt, control/README delta) recur invisibly; the kit could carry an "outstanding since vX" age tag per lane-owed line so a lane can see at a glance which follow-up has been owed longest and prioritize by age rather than re-deriving it from PR-body archaeology.

⟲ Previous-session review: the v1.14.0 upgrade session (PR #70) introduced the fixed "Lane-owed follow-ups" card section so the money-seat can grep `.sessions/` for owed items — this session reused it directly and it worked as intended (the hand-adopt-once carry-over was found in seconds). Improvement: that session left the seed-fence hand-adopt unowned for a full wave; the lane should schedule it before v1.16.0 lands or the same line ships a third time.
