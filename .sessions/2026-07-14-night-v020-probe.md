# Session — Night run: V020-null follow-through — two-version audience-separation live probe (ORDER 011 item 5)

> **Status:** `complete`

- **📊 Model:** Fable · worker slice · ORDER 011 item 5 (V020-null follow-through, night run)
- **started (date -u):** Tue Jul 14 00:31:55 UTC 2026
- **closed (date -u):** Tue Jul 14 00:40 UTC 2026
- **branch:** `claude/night-v020-probe` (PR #179)
- **session:** Run under ORDER 011 item 5 (control/inbox.md @ `991aff1`:
  "V020-null follow-through — two-version live probe measuring audience
  separation s, one night-slice budget (VERDICT 020, idea-engine
  `control/outbox.md@2808b16`)"). The verdict text was fetched at source
  (`raw.githubusercontent.com/menno420/idea-engine/2808b16/control/outbox.md`)
  and read in full — canonical over the order's one-liner. Its NULL names the
  lane's cheapest live probe verbatim: "publish two versions of ONE
  existing-research book with disjoint audience keywords and record whether
  their draws actually separate — measuring s in the wild for one
  night-slice's budget." This slice pre-registered that probe's protocol
  BEFORE preparing materials, then queued the two publish clicks + the
  post-window measurement step behind the ⚑ owner gate. Nothing external
  was published or executed; nothing was measured.
- **walls:** `candidates/night-kiln/**` and the Night Kiln series untouched
  (held by the concurrent night-kiln-3 session); no edits to manuscripts,
  `control/inbox.md`, `control/outbox.md`, workflows, or triggers; no
  publish, spend, or external action; no earnings claims (V020's own
  Q-0259 r.4 restatement honored). One coordinator-authorized exception at
  the close: the single ORDER 011 item 5 night-progress line in
  `control/status.md` (commit `1265108`).

## Results (as landed)

- **Pre-registered probe protocol** — packet #34,
  `docs/publishing/vetting/v020-probe-audience-separation.md` (commit
  `3e144e1`, landed BEFORE the materials pass by design): the two versions
  compared (*The Paper Orange* EN, graduated PR #146 ↔ *De papieren
  sinaasappel* NL, PR #131 — ONE existing-research work, keyword sets
  disjoint by the map's C4 language-namespace rule); the statistic
  `s_hat = 2·min(r_A, r_B)/(r_A + r_B)` with draws
  `r_i = net units + floor(KENP/KENPC)` over a 14-day co-launch window,
  plus a home-market attribution co-check (h_A, h_B); the owner's
  four-item post-publish data list; a mechanical decision rule
  (VOID / INCONCLUSIVE at N_min = 5 / SEPARATED / NOT SEPARATED / PARTIAL,
  one pre-registered window extension); and honest caveats — the pair is
  selected FOR separation, cross-language is the easiest axis,
  INCONCLUSIVE is the modal expected outcome at unknown-author base
  rates, s_hat is a proxy for the sim's counterfactual dial, n = 1.
  Why not the fresh #175–#178 variants: no NL packets yet + un-graduated
  EN packets = not click-ready inside one night slice (recorded in
  protocol §2).
- **Materials to the click-ready floor** (commit `538748c`): the EN
  packet's flagged §6 blurb gap CLOSED — blurb v2 + content note written
  from the finished manuscript (PR #122, 15,811 w); probe pairing notes
  (48 h co-launch, zero ads in window, no extra clicks added) in both
  packets' §7. The NL packet's listing copy was already complete; its
  blocking proofread gate stands unchanged.
- **Queue + counts (the #166 remedy class, same session)** (commit
  `1223fde`): `derive_owner_queue.py` regen 35/35 inputs clean — 19
  decisions / 33 click-run sequences / 185 owner clicks (7 hard-gated;
  the probe sequence is hard-gated via the NL proofread's blocking row);
  `lint_owner_gates.py` OK 35/35; counts-sync applied to
  `docs/current-state.md` and `docs/NEXT-SESSION.md` in the same commit.
- Heartbeat: one night-progress line for item 5 appended to
  `control/status.md` (commit `1265108`); claim removed at `19938d8`.

## ⟲ Previous-session review

previous-session review: `.sessions/2026-07-13-night-book-variants.md`
(ORDER 011 item 2, PR #172) — its walls discipline paid off directly
tonight: because it held off `docs/publishing/**` and recorded the NL
follow-through as owed instead of half-doing it, this slice could claim
that surface cleanly and regen the queue once against a stable packet set.
Its 💡 (batch the four owed NL packets into ONE regen session) is
validated by tonight's experience — even this single-packet addition moved
three count surfaces in lockstep; doing that four separate times would be
the exact churn it predicts. Honest nit: the card records the owed NL
follow-through only in its own prose and a status line — there is no
claimable artifact (no stub packet, no claims-adjacent marker) a follow-up
session would trip over, so the debt is discoverable only by reading cards
end to end.

## 💡 Session idea

💡 **Make the queue's HARD-GATED annotation say WHICH row gates the
sequence.** `derive_owner_queue.py` renders every blocked group with the
fixed suffix "(a D-item above blocks this sequence)" — but the grammar's
actual trigger is any owner box containing "blocking", and tonight's probe
sequence is gated by a *quality-gate checkbox* (the NL native-speaker
proofread), not by any D-item; the SWTK-era wording is now wrong for 1 of
the 7 hard-gated groups and will drift further as quality gates multiply.
Cheap fix inside the existing script: capture the first blocking row's
text at parse time and render "HARD-GATED — blocking row: <clipped text>"
instead of the fixed suffix. Deduped against `.sessions/2026-07-1*.md` 💡
lines: prior ideas propose new checkers/ledgers/glossaries or batching of
owed work (night-book-variants), shared-click hoisting
(nl-editions-vetting), and queue-regen batching — none touches the
HARD-GATED rendering or any derive output-accuracy fix.

## Verification

- `python3 scripts/lint_owner_gates.py` — OK, 35/35 inputs clean.
- `python3 scripts/derive_owner_queue.py` — 35/35 clean, 0 manual-review
  rows; regenerated file committed same session.
- `python3 bootstrap.py check --strict` and `python3 bootstrap.py check
  --strict --session-log .sessions/2026-07-14-night-v020-probe.md` both
  green at flip (the only prior finding was this card's own designed
  born-red HOLD).
- Claim `control/claims/2026-07-14-night-v020-probe.md` removed in commit
  `19938d8` per convention.
