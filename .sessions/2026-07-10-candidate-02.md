# Session — candidate #2 template-packs v0.1 artifact

> **Status:** `complete`

- **📊 Model:** claude-opus-4-8[1m] · high · candidate-02-template-packs
- **session:** venture-lab session 1 — build candidate #2 (agent-workflow
  template packs) to a publish-ready state alongside candidate #1's listing
- **started (date -u):** Fri Jul 10 03:21:44 UTC 2026
- **completed (date -u):** Fri Jul 10 03:22:13 UTC 2026

## Purpose

Advance candidate #2 (agent-workflow template packs) from a shortlist line to a
real, drop-in artifact: a productized slice of what the fleet already builds —
CLAUDE.md constitutions, hook sets, and session-discipline kits — with
publish-ready marketplace copy. Stop at every owner-click boundary (no
accounts, no spend, no publish).

## ⟲ Previous-session review

⟲ previous-session review: candidate #1 (membership-kit) v0.1 landed via PR #5,
shipping a mock-demonstrable product starter plus publish-ready LISTING copy
under the substrate-gate CI (green `check --strict` + a COMPLETE session card).
This session builds the *companion* product under the same gate — the workflow
layer that pairs with #1's product layer — and cross-sells it in the listing.

## 💡 Session idea

💡 The pack's moat is provenance, not novelty. Anyone can write a CLAUDE.md;
the sellable thing is that these specific rules survived a real autonomous
fleet's retros — heartbeat-before-work exists because silent sessions read as
dead, claim-before-build because two agents built the same file, the close
ritual because sessions trailed off `in-progress`. "Extracted from retros, not
aspirational" is both the differentiator and the honest reason a $19 anchor
beats a free gist: you're buying the distilled learning, not the file format.

## Log

- Synced to `origin/main` @ 33b14ce; branched `venture-lab/session-1-candidate-02`.
- Studied the repo's real artifacts to model the pack on proven material:
  `CONSTITUTION.md`, `docs/conventions.md`, `.substrate/hooks/{settings.template.json,README.md}`,
  `.sessions/` card format, `docs/AGENT_ORIENTATION.md`.
- Built `candidates/template-packs/`: README, LISTING, INCLUDED, and a drop-in
  `pack/` — `CLAUDE.md.template`, `session-card.template.md`,
  `session-discipline.md`, and `hooks/` (settings template + README + three
  advisory SessionStart/Stop/PostToolUse scripts that fail open).
- Verified the doc-hygiene surface: badge/reachability checks scan `docs/` only,
  so `candidates/**` needs no orphan link; kept the diff scoped.
- Drove `python3 bootstrap.py check --strict` to exit 0.

## Deliverable summary

A v0.1 agent-workflow template pack: a starter agent constitution, advisory
Claude Code hooks (with runnable example scripts), a session-log card carrying
the discipline markers, and a one-page discipline playbook — genericized from
the repo's own battle-tested protocol — plus publish-ready $19 marketplace copy
that cross-sells the membership-kit. Nothing owner-gated was performed; the
marketplace publish is queued as an ⚑ owner action.
