# Session — The Salt Bell (concept #3) written to owner-click-ready sellable

> **Status:** `complete`

- **📊 Model:** Claude Opus · high · feature build
- **started (date -u):** Thu Jul 16 15:59:56 UTC 2026
- **branch:** `claude/salt-bell` (PR TBD)
- **session:** Coordinator-dispatched BOOKS-lane write-slice — the next
  adult novella from the on-file shortlist
  (`docs/ideas/2026-07-14-adult-title-concepts.md`), written all the way to
  an **owner-click-ready sellable in one slice**: manuscript +
  verified-vs-invented DECISIONS ledger + EN vetting packet + keyword-map
  rows + owner-queue regen + counts-sync. This is **ORDER 011 item 2's
  named next concept** — the shortlist's next-slice pointer: **#3 The Salt
  Bell (24/30)**, Zeeland, the North Sea flood of 31 January–1 February
  1953 and the reconstruction after. Net-new sellable that needs NO owner
  click to build (product value; the six publish clicks stay ⚑ owner-gated).
- **sensitivity:** The Salt Bell depicts a real disaster (the 1953
  Watersnoodramp — 1,836 dead in the Netherlands, living memory). House
  Paper-Orange-grade care applied per the `DECISIONS.md` "Sensitive-history
  care" convention (Paper Orange / Sweetwater / Wire Garden precedent):
  fictional village (Zoutkerke) and fictional characters, no real victim
  named or staged, factual backdrop grounded accurately (date, storm surge
  on spring tide, dike failures, the caisson closures, the Delta Works), a
  brief respectful author's note distinguishing the fiction from the real
  event, and a front-matter content note. The village counts only its own
  dead — the province-wide toll is never flattened to one number in prose.
- **scope:** `candidates/adult-novels/the-salt-bell/{en/the-salt-bell.md,
  DECISIONS.md}` (12-chapter novella, house band ~15,000–16,000 words,
  honest `wc -w` from the file) + `docs/publishing/vetting/the-salt-bell.md`
  (manuscript-first packet, same-slice) + `docs/publishing/keyword-map.md`
  (2 categories + 7 keywords, C3 sixth Netherlands era-register, first-claim
  scan per V057) + ONE `docs/publishing/OWNER-QUEUE.md` regen via
  `scripts/derive_owner_queue.py` (never hand-edited) + counts-sync
  (`docs/current-state.md`, `docs/NEXT-SESSION.md`) + shortlist marked #3
  WRITTEN and its stale next-slice pointer fixed.
- **walls:** no publish, spend, or external action; no edits to
  `control/inbox.md`; no merge or auto-merge from this seat; the generated
  OWNER-QUEUE.md is only ever written by the script; family-level model
  names only; forward-only git (no force-push/rewrite); honest word counts
  from the real file, never estimated.
- **verify plan:** re-measure `wc -w` on the committed manuscript after each
  3-chapter chunk; run `scripts/lint_owner_gates.py` (must pass);
  regenerate the queue and diff it (the ONLY new drift must be the Salt Bell
  sequence + era-register rows); `python3 bootstrap.py check --strict` must
  exit 0 (its born-red HOLD on this card is the designed exception until
  flip); commit the `.substrate/guard-fires.jsonl` telemetry delta, do not
  revert.
- **done-when:** the manuscript + DECISIONS + packet + keyword rows +
  regenerated queue + counts-sync + telemetry land on a READY PR, CI green
  (kit-tests + substrate-gate), heartbeat re-stamped, and this card flipped
  `complete` as the last commit (clearing the born-red HOLD).

## ⟲ Previous-session review

previous-session review: `.sessions/2026-07-16-owner-queue-blocker-reason.md`
(PR #210, merged as `acdbf2d` — the boot HEAD of this slice). That slice
fixed `derive_owner_queue.py` so hard-gated sequences render their REAL
blocking row instead of a phantom `(a D-item above blocks this sequence)`
suffix; its central facts held under re-verification this wake (main HEAD at
boot was exactly `acdbf2d`, inbox still ends at ORDER 015, the owner queue
described 44 sequences / 16 hard-gated pre-Salt-Bell). ONE executable rule
transferred and executed verbatim: **`OWNER-QUEUE.md` is only ever written
by the script, never hand-edited** — this slice ran exactly one
`derive_owner_queue.py` regen and committed only its 9-line output (the Salt
Bell sequence), diffing to confirm zero other drift; The Salt Bell landed
UNBLOCKED, so it added no hard-gated row and left that slice's corrected
suffix set untouched. Honest note on its 💡 (anchor the blocking row
in-place with a `◀ THIS is the gate` marker): still unclaimed — this
BOOKS-lane slice touched the queue only through the script and had no reason
to open the renderer, so the anchor idea remains a live next-slice pickup
for whoever next works the generator.

## 💡 Session idea

💡 **The catalog's hand-maintained counts want a `derive_catalog_counts.py`,
the way the owner queue got `derive_owner_queue.py`.** This slice's
counts-sync had to touch the same three numbers (adult titles / complete EN
manuscripts / vetting packets) plus the owner-queue totals by hand in FOUR
prose sites across two files (`docs/current-state.md` ×3 and
`docs/NEXT-SESSION.md` ×2), each with its own dated-snapshot phrasing — the
exact multi-site drift risk that `derive_owner_queue.py` was built to kill
for the publish queue. Cheap next slice: a tolerant, advisory,
stdlib-only script that greps the real tree (`candidates/adult-novels/*/en/`,
`docs/publishing/vetting/*.md`, the derived queue totals) and prints the
honest counts, so a session re-derives them instead of copying prose forward
and a checker can NAG when a hand-written count trails the tree (the
`check_ledger_drift.py` pattern, extended from "recently shipped" to "catalog
counts"). Deduped against prior `.sessions/*.md` 💡 lines: the
blocker-reason card's is intra-sequence gate anchoring; the earlier cohort
ideas are the per-concept outcome table and the NL-title pre-naming — none
touches how the catalog's summary counts are kept from rotting. (This slice
executed the pre-named-NL convention already: *De zoute klok* is in
DECISIONS.md.)

## Results (as landed)

- **Manuscript delivered:**
  `candidates/adult-novels/the-salt-bell/en/the-salt-bell.md` — **The Salt
  Bell**, subtitle *A novella of the Zeeland flood*, 12 chapters + author's
  note, close third on Marinus de Ridder (koster/klokkenist of the invented
  Zeeland village Zoutkerke), the 1953 North Sea flood and the
  reconstruction after. Honest count, verbatim, re-measured from the
  committed file at the final content commit: `wc -w` = **15,212** — inside
  the 15,000–16,000 band, low end. Drafted in 3-chapter chunks (`3e22e60`
  ch1–3, `9d99e1b` ch4–6, `9781267` ch7–9, `5ea0f90` ch10–12 + author's note
  + six under-served beats written in as finished prose to reach the band,
  the Sweetwater precedent — draft history recorded in DECISIONS.md, not
  padded).
- **Historical spine ledgered:**
  `candidates/adult-novels/the-salt-bell/DECISIONS.md` (`2a4c7c8`) —
  verified 1953-flood spine (night of 31 Jan–1 Feb, NW storm surge on a
  spring tide, dike failures, the failed/late warning, church bells as the
  only alarm, the improvised rescue armada, the caisson closures, the Delta
  Works, the wartime *klokkenroof* backstory, "Luctor et Emergo") separated
  from marked inventions (Zoutkerke + the Sint-Maartenspolder wholly
  invented; all characters fictional; no real person staged, no real victim
  named). NL edition pre-named *De zoute klok*.
- **Sensitive-history care (real disaster, 1,836 dead, living memory):**
  Paper-Orange-grade — fictional village and people, the province-wide toll
  never flattened to one number in prose (the village counts only its own),
  drownings witnessed with restraint, a front-matter content note, and a
  closing author's note distinguishing the fiction from the real
  Watersnoodramp and honouring the real dead without appropriating them.
- **Graduated to owner-click-ready IN ONE SLICE** (the inverse of the
  Sweetwater/Wire deferred-packet order): EN vetting packet
  `docs/publishing/vetting/the-salt-bell.md` + 2 categories / 7 keywords in
  `keyword-map.md` §1 (sixth Netherlands era-register, C3; first-claim scan
  clean per V057) + ONE `derive_owner_queue.py` regen of `OWNER-QUEUE.md`
  (adds the 45th sequence, unblocked, 6 clicks — 19 decisions / 45 sequences
  / 268 clicks / 16 hard-gated, 47/47 inputs clean) + counts-sync
  (`current-state.md`, `NEXT-SESSION.md` → adult novels 13 titles / 15 EN
  manuscripts; 46 vetting packets) + shortlist concept #3 marked WRITTEN and
  its stale next-slice pointer fixed (→ #4 The Lamp Room). Commit `74c8606`.
- **Gates:** `scripts/lint_owner_gates.py` → `OK — 47 input(s) clean`;
  `python3 bootstrap.py check --strict` green except this card's own
  designed born-red HOLD (now cleared by this flip); the 11-record
  guard-fire telemetry delta committed with the build per convention, not
  reverted.
- **ONE heartbeat** re-stamp (`control/status.md`, `357af9a`);
  `control/inbox.md` untouched.
- **Landed as PR #211** (READY, base main, head `claude/salt-bell`).
  Auto-merge left to the enabler workflow, untouched by this seat. The
  recorded follow-up — the NL edition *De zoute klok* and its packet — is
  seat work, not owner clicks.
- The `📊 Model:` line is well-formed: `Claude Opus · high · feature build`
  (family-level model · taxonomy effort · PL-004 task class).
