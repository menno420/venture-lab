# Session — kit upgrade v1.12.0 → v1.12.1

> **Status:** `complete`

- 📊 Model: fable-5
- **started (date -u):** 2026-07-11 22:25:23 UTC
- **scope:** kit-owned files only — vendored `bootstrap.py`, `.substrate/`
  state, kit-owned planted docs, and this card. No `control/inbox.md` /
  `control/status.md` writes this session (hard scope; heartbeat kit-bump is
  lane-owed, not this PR).

## Purpose

Upgrade substrate-kit v1.12.0 → v1.12.1 (distribution wave). Canonical
recipe: stage `bootstrap.py.new` + `release.json` in repo root →
`python3 bootstrap.py.new upgrade` (self-verifies sha256+version,
self-cleans staged inputs) → verify banks/stamps/carve-out →
`bootstrap.py check --strict` exit 0. This ADDED card rides the locked
born-red door (HOLD until the deliberate final flip).

## Log

- Born-red heartbeat: card + claim were the session's first commit
  (`e055881`); PR #56 opened immediately after. Flip to `complete` is the
  deliberate last step.
- Preflight: origin/main at `2044dc6` — dist header v1.12.0, pre-upgrade
  `bootstrap.py` sha256 `77c00b81…e1f8` == v1.12.0 release asset; all three
  version stamps at 1.12.0. Matches the wave-briefing snapshot exactly.
- Upgrade commit `4776045`: canonical `bootstrap.py.new upgrade` with
  adjacent release.json; asset sha256 `1055ca2c…e4aa` (704,108 bytes) and
  release.json (449 bytes, own digest `cfce501f…b9a7`) verified before use;
  engine self-verified ("verified: sha256 + version against release.json")
  and self-cleaned both inputs.

## Verification record

- **Carve-out scan — VERDICT: CLEAN.** Engine stdout verbatim:
  `upgrade: carve-out scan: .github/workflows/substrate-gate.yml — ran, 0 found (live matched the previous kit template byte-for-byte; the delta is kit-side template evolution)`
  No phantom bank: backup dir gained ONLY `bootstrap-1.12.0.py`; the
  historical `substrate-gate.pre-regen-4f50eb4d.yml` untouched.
- **Live-gate regen:** `.github/workflows/substrate-gate.yml` regenerated
  from the v1.12.1 template (+23/−4: sibling-card locked door supersedes
  #187 advisory semantics, deleted-card hard red — kit review #226
  G-1/G-2). Live gate byte-identical to staged
  `.substrate/ci/substrate-gate.yml` (diff empty).
- **Backup bank:** exactly one new archive
  `.substrate/backup/bootstrap-1.12.0.py`, sha256
  `77c00b811429e1b526ccc7e0dcf597435c11048e16a67edba6050f516ad5e1f8`
  == pre-upgrade dist == v1.12.0 release asset. All 8 pre-existing
  bootstrap banks byte-identical before/after (sha256 baseline taken
  pre-branch); only `backup/last-upgrade.json` + `backup/state.json`
  rewritten by the engine as designed — PASS.
- **Version stamps:** bootstrap.py header v1.12.1, `.substrate/state.json`
  kit_version 1.12.1, `substrate.config.json` 1.12.1; vendored dist sha256
  `1055ca2c…e4aa` == release asset — PASS.
- **Planted docs:** all 23 kept ("consumer-owned" / already current — incl.
  control/inbox.md + control/status.md untouched); staged `.substrate/`
  copies refreshed (CLAUDE.md, skills, agents, hooks, ci) — PASS.
- **Mid-session `check --strict`:** reported ONLY the designed born-red
  HOLD (verbatim "HOLD (by design) … nothing to investigate; nothing to
  investigate") plus this card's own missing-enders line — both resolved by
  this flip. Final CI green on the flipped head is the exit-0 proof.
- **Designed-red CI:** substrate-gate run 29170528723 failed on upgrade
  head `4776045` — the born-red hold working as designed (the run executed
  the NEW regenerated gate from the PR branch).
- **guard-fires.jsonl:** tracked, gained one appended line (this session's
  own mid-session check run) — committed with this flip as audit trail.
- **HANDOFF discipline:** no HANDOFF.md present, none fabricated — PASS.

## ⟲ Previous-session review

Previous-session review: the v1.12.0 upgrade session (PR #42) left an
excellent verification record — its explicit before/after sha256 baseline
of every banked archive is what made this session's bank-integrity check a
two-minute diff instead of a re-derivation, and its claim-format
mid-session fix meant this session wrote a parseable claim bullet on the
first try by copying its corrected grammar. What it flagged but couldn't
fix — the dangling `docs/AGENT_ORIENTATION.md` → `.claude/CLAUDE.md`
pointers (repo has only the staged `.substrate/claude/CLAUDE.md`) — is
STILL dangling after v1.12.1: this release did not ship the
orientation-pointer fix, so the ⚑ stands for a second consecutive wave.
Workflow improvement: the wave briefing should carry a "known-open kit
follow-ups from last wave" line per repo so the distribution worker
re-checks them against the new release deliberately instead of
rediscovering them via the previous card.

## 💡 Session idea

💡 Session idea: teach `bootstrap.py upgrade` to print a one-line
**gate-delta summary** whenever it regenerates the live
substrate-gate.yml — e.g. `gate regen: +sibling-card locked door,
+deleted-card hard red (template v1.12.1)` — sourced from a short
changelog stanza embedded in the template itself. Today the worker must
read the raw yml diff to learn what merge-blocking semantics just changed
under the repo's feet; a semantic summary line makes the CI-behavior
change auditable from the upgrade report alone, which matters because the
gate is the one kit-owned file that can newly red-block *other* lanes'
in-flight PRs the moment this PR merges.
