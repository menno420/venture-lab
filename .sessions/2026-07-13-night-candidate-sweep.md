# Session — Night run: candidate sweep + Kill-Rule Intake Kit to the owner gate (ORDER 008, PRODUCT #7)

> **Status:** `in-progress`

- **📊 Model:** fable-5 · worker slice · ORDER 008 night-run product lane
- **session:** PRODUCT #7 of the 2026-07-13 night run — sweep the four
  unpacketed candidates (`cc-cost-lens`, `kill-rule-intake-kit`,
  `merge-wall-cookbook`, `false-green-test-trap`), rank by
  distance-to-READY, and drive the closest one to publish-READY per
  `docs/products/TEMPLATE.md` floor 6/6 (all executed).
- **started (date -u):** Sun Jul 13 02:26:08 UTC 2026
- **closed (date -u):** (in progress)

## Scope (planned)

- Sweep verdicts, one line each, into `docs/current-state.md`.
- Drive the top-ranked candidate: build + `package.sh` + double-rebuild
  sha proof + honest-null substitute executed from the extracted bundle +
  price with precedent chain + `docs/launch/<name>/` listing copy at
  catalog parity + six-field owner-actions + §7 packet
  `docs/publishing/vetting/<name>.md` + OWNER-QUEUE regen
  (baseline-then-delta proof).
- If all four turn out idea-only: no fabricated product; implement the
  queue ⏲/KILL-CHECK column instead.
