# 2026-07-13 — NL-edition vetting rows: Het trage woord + De papieren sinaasappel

> **Status:** `complete`

One-line summary: vetting packets + keyword-map rows for the catalog's two
landed Dutch editions — *Het trage woord* (PR #126) and *De papieren
sinaasappel* (PR #130) — closing the "NL listings need their own vetting
rows" ⚑ follow-up both NL NOTES.md files carry.

Started: Mon Jul 13 03:13:57 UTC 2026 (born-red first commit `6b5eef0`).
Closed: Mon Jul 13 03:19:33 UTC 2026. PR #131.

## Intent

Run under ORDER 008 (owner night run 2026-07-13, BOOKS clause). Both NL
editions landed with an explicit flag: "An NL listing needs its own
vetting/keyword rows in `docs/publishing/` — the existing vetting packet
covers the EN title only" (see
`candidates/adult-novels/the-slow-word/versions/novella-cut-nl/NOTES.md` and
`candidates/adult-novels/the-paper-orange/versions/nl/NOTES.md`, ⚑
follow-ups). This slice does exactly that work: one vetting packet per NL
edition (CHECKLIST one-pass, dated 2026-07-13 collision scans on the Dutch
titles), keyword-map ownership rows for the Dutch-language phrases, and the
derived owner queue regenerated so the new §7 ⚑ OWNER blocks surface.

## Scope

One work increment, `docs/publishing/**` only (plus this card + claim):

- `docs/publishing/vetting/het-trage-woord.md` — NL-edition packet.
- `docs/publishing/vetting/de-papieren-sinaasappel.md` — NL-edition packet.
- `docs/publishing/keyword-map.md` — NL-language ownership rows + a
  Dutch-language-namespace rule alongside C3.
- `docs/publishing/OWNER-QUEUE.md` — regenerated by
  `scripts/derive_owner_queue.py` (never hand-edited).

Premise verified at HEAD (`2d972ed`) before building: no vetting packet or
keyword-map row exists for either Dutch title (repo-wide grep for "trage
woord" / "papieren sinaasappel" hits only candidates/** and .sessions/**);
no live claim covers docs/publishing/. Walls held: no publish, spend, or
external action; manuscripts untouched; control/status.md, outbox, inbox
untouched.

## Outcome

- Two NL-edition vetting packets, both `plan`, both publish-ready up to
  the §7 owner gate, each with a dated (2026-07-13) collision scan run
  this session: *Het trage woord* → **None** (no same-named Dutch book;
  nearest hit a different phrase, *Trage wegen*); *De papieren
  sinaasappel* → **Low** (no exact-title book; Harris's *Vijf kwarten van
  de sinaasappel* logged as intent-adjacent, the active Hongerwinter
  shelf — Currie YA, De Zwarte/Barnouw nonfiction — logged as a demand
  signal, not a collision).
- Honest gaps flagged, not skipped: NL priced comps NOT pulled; €4.99
  bands inherited from the EN packets, not NL-re-derived; native-speaker
  proofread queued as an explicit §7 row (weighted extra for the
  Hongerwinter title's living-memory audience).
- Keyword map: 14 Dutch keyword rows + 2 WWII category rows + new **C4
  standing rule** (Dutch-language phrases are a separate namespace;
  same-title editions share browse nodes; C3's era-register split extends
  into Dutch; "Oorlogswinter" never drafted — it is Terlouw's title).
- `OWNER-QUEUE.md` regenerated (21/21 inputs clean, the two new click-run
  sequences surface, manual-review none). `bootstrap.py check --strict`:
  only the designed born-red HOLD pre-flip.
- Follow-up left open (deliberately out of scope): graduating the EN
  *Paper Orange* concept packet now its manuscript exists (PR #122).

## 💡 Session idea
💡 **Shared-click hoisting in the derived owner queue.** The regenerated
queue now carries 112 owner clicks across 19 sequences, and the SAME
clicks recur verbatim in almost every book packet's §7 — "KDP account +
tax/bank interview" appears in every KDP sequence, cover-composition
reuse pairs (EN/NL type layers) appear in both editions of both
translated titles. `derive_owner_queue.py` could detect repeated click
text across sequences and hoist it into a "do once, unblocks N
sequences" section at the top, so the owner sees that ONE tax-interview
click unblocks every book in the catalog instead of reading it queued
eight times. Distinct from the existing ⏲/KILL-CHECK and DONE-disposition
ideas (those annotate rows; this reorders by shared-prerequisite count).

## Previous-session review
previous-session review: `.sessions/2026-07-13-paper-orange-nl.md`
(PR #130) — genuine strength: its NOTES.md carried exactly the measured
facts this packet needed (verbatim `wc -w`, locked title decision with
rejected alternatives, market-native argument, the pre-flagged follow-up
naming this very slice), so the packet work was assembly, not research;
honest nit: its ⚑ follow-up said "NL listings need their own
vetting/keyword rows" without confronting that the keyword map had no
concept of a second LANGUAGE namespace — this slice had to invent the C4
rule the follow-up silently assumed, which is the kind of convention gap
worth naming when you flag work for a successor.

## Model
- **📊 Model:** fable-5 · worker slice · ORDER 008 night-run books lane

Run under ORDER 008.
