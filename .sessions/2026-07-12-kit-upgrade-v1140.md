# 2026-07-12 — kit upgrade v1.13.0 → v1.14.0

> **Status:** `complete`

Upgraded substrate-kit v1.13.0 → v1.14.0 (distribution-only wave; no lane domain work).

📊 Model: fable-5

## What shipped

- `bootstrap.py` swapped to v1.14.0 dist (three-way sha256 verified: download = release.json = substrate-kit origin/main dist = `47c1b8b9…e581`, 779399 bytes).
- Old dist banked at `.substrate/backup/bootstrap-1.13.0.py`; all 10 pre-existing banks byte-identical.
- `--apply-docs` applied 4 template-improved docs: CONSTITUTION.md, docs/collaboration-model.md, docs/question-router.md, docs/SKILLS.md.
- Payload: /intake skill, owner-assist paste-ready output standard, venue-scoped capability ledger.

## Lane-owed follow-ups (not forced by this distribution PR)

- capability-seed hand-adopt-once on docs/CAPABILITIES.md (seed fence adoption — see PR #70 body, verbatim line).
- Diverged manual merges: docs/CAPABILITIES.md, control/README.md.
- Q-0270 local-prose-copy collapse check.

💡 Session idea: the upgrade report's "hand-adopt once" capability-seed line has now recurred across adopters — the kit could ship a `bootstrap upgrade --adopt-seed` helper that performs the fence splice mechanically (keep append log below, replace seed sections), turning a lane-owed manual merge into a one-flag operation.

⟲ Previous-session review: the v1.13.0 upgrade session (PR #64) verified the enabler auto-squash-merge behavior and recorded it, which made this session's no-self-arm/no-self-merge posture unambiguous — good precedent capture. Improvement: upgrade session cards should carry the lane-owed list in a fixed section (as this one now does) so the money-seat can grep `.sessions/` for owed follow-ups instead of re-reading PR bodies.
