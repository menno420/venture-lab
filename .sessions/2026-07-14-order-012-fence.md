# Session — ORDER 012: capability-seed fence restore + walls fold

> **Status:** `complete`

- **📊 Model:** Claude Fable · order-012 fence restore + walls fold
- **session:** ORDER 012 — restore the kit `substrate-kit:capability-seed` fence in `docs/CAPABILITIES.md`, fold the real verified walls from `docs/PLATFORM-LIMITS.md` into the ledger venue-tagged so the seat-digest walls block stops rendering empty, regenerate `docs/seat-digest.md`
- **started (date -u):** Tue Jul 14 06:31:38 UTC 2026
- **closed (date -u):** Tue Jul 14 06:35:30 UTC 2026

## ⟲ Previous-session review

Previous-session review: the 2026-07-14 night wave closed clean — ORDER 011's
worklist fully consumed (11 PRs enabler-merged, night tally on the heartbeat
at 04:01:48Z), OWNER-QUEUE regens counts-synced, nothing reverted or
re-opened by this session. The one standing gap this session exists to fix
was structural, not regression: the capability ledger never carried the kit
fence, so the seat digest under-reported this seat as wall-free (fm
fence-index @ `3b335a8`, INC-48 class).

## 💡 Session idea

💡 Deduped idea: mechanize the fence-exposure probe locally — a one-line CI
advisory (grep for `substrate-kit:capability-seed BEGIN` in
`docs/CAPABILITIES.md`) would have caught this fence's absence at the first
regen instead of waiting for the fleet's cross-repo index sweep; propose it
upstream to the kit as a `check` advisory rather than a lane-local script
(the fm fence-index already flags the same follow-up manager-side, so the
kit checker is the non-duplicating home).

## Scope (ORDER 012)

- Restore the kit-form `substrate-kit:capability-seed` fence in
  `docs/CAPABILITIES.md` (reference: superbot-next `docs/CAPABILITIES.md:19`/`:101`,
  verified byte-identical to this repo's own `bootstrap.py` template render).
- Fold the PLATFORM-LIMITS verified walls (tag-push/Release/branch-delete 403 ·
  no-API owner-click surfaces · self-merge classifier wall with the
  agent-unlandable-PR recipe · direct-push-to-main ruleset) into the ledger
  venue-tagged the way the digest extractor expects.
- One extractable source of truth; regenerate `docs/seat-digest.md` via
  `python3 bootstrap.py seat-digest`; `python3 bootstrap.py check --strict` green.

## Work log

- **Reference chase (both fetches SUCCEEDED):** superbot-next
  `docs/CAPABILITIES.md` @ main fetched over raw.githubusercontent.com —
  fence markers at lines 19/101 exactly as the ORDER cites; fm
  `docs/fence-index.md` @ `3b335a8` fetched — confirms venture-lab's row
  ("❌ absent · INC-48 class") and the three-fence extraction contract.
- **Fence restored in KIT FORM:** rather than hand-copying, the fence block
  was rendered from this repo's own `bootstrap.py` embedded
  `CAPABILITIES.md.tmpl` (project_name=venture-lab) and verified
  byte-identical to the superbot-next reference (diff: IDENTICAL), then
  verified in-place with `_capability_fence(doc) == _capability_fence(render)`
  → True. Future `upgrade` runs will classify the fence "already current"
  and keep refreshing it — nothing consumer-edited sits inside the fence.
- **Walls folded venue-tagged (append log, the digest's second extraction
  source):** the fleet-generic walls (tag-push/Release 403, branch-delete,
  api.github.com, owner-click console surfaces, GraphQL quota) ride the seed
  fence's own `any` rows; the venture-lab-specific walls landed as four
  dated 6-field append entries — self-merge classifier + agent-unlandable-PR
  recipe (`autonomous-project`; PRs #9/#15/#55 denials, enabler recipe),
  direct-push-to-main 422 ruleset (`any`), branch-delete 403 re-verify with
  the verbatim RPC error (`any`), and the no-API owner-click surfaces incl.
  GitHub repo settings/rulesets (`any`). Local capability recipes preserved
  in a consumer-owned section below the fence; stale local guidance ("arm
  auto-merge at creation") corrected to the proven enabler path.
- **Source-of-truth call:** `docs/PLATFORM-LIMITS.md` CONVERTED to a pointer
  + verbatim-evidence appendix (all classifier/403/422 verbatim texts
  preserved, keyed per ledger wall). Rationale: leaving it as-is keeps two
  normative walls lists and re-invites the exact INC-48 drift this ORDER
  fixes; the ledger line stays digest-sized while oversized verbatim
  evidence parks in the appendix.
- **Digest regenerated:** `python3 bootstrap.py seat-digest` — the walls
  block now renders 7 rows + "…plus 2 more — read `docs/CAPABILITIES.md`."
  and no longer contains "(no walls recorded for these venues)".
- **Gate:** `python3 bootstrap.py check --strict` — green apart from the
  designed born-red hold on this card, cleared by this flip commit.

## Status / outcome

Complete. Fence present in kit form; walls digest non-empty (tag-push 403 ·
branch-delete · api.github.com · owner-click surfaces · GraphQL · self-merge
agent-unlandable recipe · direct-push ruleset, + 2 overflow rows);
PLATFORM-LIMITS demoted to evidence appendix; heartbeat ack appended to
`control/status.md`. Landing rides the auto-merge enabler on the
`claude/order-012-fence` head per conventions — no self-merge, no arm.
