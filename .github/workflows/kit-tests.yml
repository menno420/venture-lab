# HOST-OWNED companion to substrate-gate. The gate workflow is
# KIT-OWNED (adopt/upgrade regenerates it; hand edits are overwritten
# and its header says host customizations belong in a SEPARATE file) —
# so the candidate test suites run here instead.
# Purpose: make "real-path HTTP-layer tests GREEN in CI" a named,
# enforced check — runs the membership-kit server suites, including
# the ORDER 003 real-path HTTP tests (test_http_realpath), which start
# the actual app.py server and drive signed Stripe fixture payloads
# over real HTTP. Stdlib-only, no external deps. unittest exits
# nonzero on any failure, so a failing test reds this check.
# (template-packs ships no test suite at HEAD; add it here when one
# exists.)
name: kit-tests
on:
  pull_request:
  push:
    branches: [main]
jobs:
  membership-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: membership-kit server tests (unit + ORDER 003 real-path HTTP + SupabaseStore)
        working-directory: candidates/membership-kit/server
        run: python3 -m unittest test_membership test_http_realpath test_supabase_store -v
  stripe-webhook-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: stripe-webhook-test-kit real-path HTTP tests
        working-directory: candidates/stripe-webhook-test-kit
        run: python3 -m unittest test_http_realpath -v
  github-webhook-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: github-webhook-test-kit real-path HTTP tests
        working-directory: candidates/github-webhook-test-kit
        run: python3 -m unittest test_http_realpath -v
  slack-webhook-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: slack-webhook-test-kit real-path HTTP tests
        working-directory: candidates/slack-webhook-test-kit
        run: python3 -m unittest test_http_realpath -v
  shopify-webhook-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: shopify-webhook-test-kit real-path HTTP tests
        working-directory: candidates/shopify-webhook-test-kit
        run: python3 -m unittest test_http_realpath -v
  idempotency-key-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: idempotency-key-test-kit real-path HTTP tests
        working-directory: candidates/idempotency-key-test-kit
        run: python3 -m unittest test_http_realpath -v
  rate-limit-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: rate-limit-test-kit real-path HTTP tests
        working-directory: candidates/rate-limit-test-kit
        run: python3 -m unittest test_http_realpath -v
  pagination-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: pagination-test-kit real-path HTTP tests
        working-directory: candidates/pagination-test-kit
        run: python3 -m unittest test_http_realpath -v
  jwt-auth-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: jwt-auth-test-kit real-path HTTP tests
        working-directory: candidates/jwt-auth-test-kit
        run: python3 -m unittest test_http_realpath -v
  cors-preflight-test-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: cors-preflight-test-kit real-path HTTP tests
        working-directory: candidates/cors-preflight-test-kit
        run: python3 -m unittest test_http_realpath -v
  idempotency-retry-cookbook-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: idempotency-retry-cookbook recipe self-test (backoff + idempotency store + retry executor)
        working-directory: candidates/idempotency-retry-cookbook/recipes
        run: python3 -m unittest test_recipes -v
  webhook-verifier-bundle-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: webhook-verifier-bundle assembly/inventory check (manifest + pinned component shas + built zip)
        working-directory: candidates/webhook-verifier-bundle
        run: python3 -m unittest test_bundle -v
  api-robustness-bundle-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: api-robustness-bundle assembly/inventory check (manifest + pinned component shas + built zip)
        working-directory: candidates/api-robustness-bundle
        run: python3 -m unittest test_bundle -v
  owner-click-queue-kit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: owner-click-queue-kit parse/derive/lint tests
        working-directory: candidates/owner-click-queue-kit
        run: python3 -m unittest test_ocq -v
  catalog-dref-guard:
    # REQUIRED gate. Machine-catches the OWNER-QUEUE renumber-mispoint class
    # (PR #244 renumbered decision IDs and the bundle/catalog D-ref
    # cross-references silently mispointed; bootstrap --strict is semantic-blind
    # to cross-refs, so #244 shipped green and #245 hand-resynced). The checker
    # builds the live D-ID → SKU map from OWNER-QUEUE.md §1 and asserts every
    # allowlisted live cross-ref resolves + points at the right SKU; the unittest
    # adds a catch-case fixture proving the guard fires. Stdlib-only, system
    # python3. Contract + allowlist/exclusion scoping: the
    # scripts/check_catalog_drefs.py docstring.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: catalog D-ref guard (checker on the live tree)
        run: python3 scripts/check_catalog_drefs.py
      - name: catalog D-ref guard tests (pass-case on the tree + mispoint catch-case)
        working-directory: scripts
        run: python3 -m unittest test_check_catalog_drefs -v
  owner-gate-lint-advisory:
    # ADVISORY ONLY — never a required gate. The step is
    # continue-on-error, so a red lint flags without blocking unrelated
    # work (same shape as ledger-drift-advisory below). It runs the
    # strict OCQK-ported linter over the packet §7 OWNER-GATE blocks +
    # keyword-map ⚑ OWNER conflicts and exits 1 on any malformed gate —
    # including date-SHAPED but impossible dates (month 13, day 32)
    # that the tolerant derive script accepts. Contract + checks:
    # the scripts/lint_owner_gates.py docstring.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: owner-gate lint advisory (stdlib-only, system python3)
        continue-on-error: true
        run: python3 scripts/lint_owner_gates.py
  ledger-drift-advisory:
    # ADVISORY ONLY — never a required gate. Double-guarded against
    # ever turning red: the script exits 0 on EVERY path (in-sync,
    # trailing, skip, parse failure) AND the step is continue-on-error.
    # It prints one ledger-drift line comparing docs/current-state.md
    # "Recently shipped" vs the newest merged PR. Contract + usage:
    # docs/ledger-drift-checker.md.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ledger drift advisory (stdlib-only, system python3)
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/check_ledger_drift.py
  funnel-coverage-advisory:
    # ADVISORY ONLY — never a required gate. Double-guarded against ever
    # turning red: the script exits 0 on EVERY path (covered, uncovered,
    # skip) AND the step is continue-on-error (same shape as
    # ledger-drift-advisory above). It reads docs/launch/CATALOG.md
    # §"Cross-sell clusters" and prints one line per cluster, warning
    # UNCOVERED on any cluster that carries a singles/bundle list but has
    # no linked *-lead-magnet.md funnel-top — the "which cluster is the
    # next magnet target" signal. The unittest adds an uncovered-fixture
    # catch-case proving the advisory fires. Contract + coverage model:
    # the scripts/check_funnel_coverage.py docstring.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: funnel-coverage advisory (stdlib-only, system python3)
        continue-on-error: true
        run: python3 scripts/check_funnel_coverage.py
      - name: funnel-coverage advisory tests (live-tree green + uncovered catch-case)
        continue-on-error: true
        working-directory: scripts
        run: python3 -m unittest test_check_funnel_coverage -v
